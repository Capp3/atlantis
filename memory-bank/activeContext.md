# Memory Bank: Active Context

## Current Focus
**No active task** — Run **`/van`** to pick optional follow-ups.

## Project State (2026-05-16)
- **MVP + Phase 7/8 + P8-H complete** — editor, offline preview, persistence, front matter, plugins scaffold, PyInstaller local + CI bundle workflow
- **107 tests**; **80%** cov; `make check-all` green
- **14 archives** in `memory-bank/archive/`

## Optional Backlog
Export plugin, FS-F cache, FM polish, macOS bundle, tag-triggered Release

## Post-merge (optional)
Dispatch **Bundle Linux (experimental)** once to validate remote runner + artifact

## Last Archive
`memory-bank/archive/archive-p8h-bundle-ci-2026-05-16.md`
